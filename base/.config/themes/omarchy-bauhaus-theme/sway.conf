# Omarchy Bauhaus theme for Sway

# Color definitions
set $bg #101318
set $fg #EAEFF5
set $inactive #3a3f4c
set $accent #e37b66
set $red #CB886D
set $green #789FA2
set $yellow #E7A46F
set $blue #8999AA
set $magenta #8D758F
set $cyan #809D9E

# Window colors           border      bg          text        indicator   child_border
client.focused            $accent     $bg         $fg         $yellow     $accent
client.focused_inactive   $inactive   $bg         $fg         $inactive   $inactive
client.unfocused          $inactive   $bg         $fg         $inactive   $inactive
client.urgent             $red        $bg         $fg         $red        $red
client.placeholder        $inactive   $bg         $fg         $inactive   $inactive
client.background         $bg

# SwayFX-specific settings
shadows enable
shadows_on_csd enable
shadow_blur_radius 20
shadow_color #00000099

blur enable
blur_passes 2
blur_radius 5
