# Frost theme for Sway

# Color definitions
set $bg #0a0f1c
set $bg_alt #0f1425
set $fg #d4d5d9
set $inactive #5f6374
set $accent1 #A8B9C6
set $accent2 #95A8B8
set $blue #9BB0C2
set $cyan #B8C7D0
set $red #869AAC

# Window colors           border      bg          text        indicator   child_border
client.focused            $accent1    $bg         $fg         $accent2    $accent1
client.focused_inactive   $inactive   $bg         $fg         $inactive   $inactive
client.unfocused          $inactive   $bg         $fg         $inactive   $inactive
client.urgent             $red        $bg         $fg         $red        $red
client.placeholder        $inactive   $bg         $fg         $inactive   $inactive
client.background         $bg

# SwayFX-specific settings
shadows enable
shadows_on_csd enable
shadow_blur_radius 20
shadow_color #00000088

blur enable
blur_passes 2
blur_radius 5
