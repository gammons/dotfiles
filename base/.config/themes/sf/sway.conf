# SparkFabrik (sf) theme for Sway

# Color definitions
set $bg #031527
set $fg #ffffff
set $inactive #0c335a
set $dark #02101d
set $red #eb0000
set $yellow #f7ad2c
set $orange #f36931
set $blue #027aca
set $cyan #40c6cf

# Window colors           border      bg          text        indicator   child_border
client.focused            $red        $bg         $fg         $cyan       $red
client.focused_inactive   $inactive   $bg         $fg         $inactive   $inactive
client.unfocused          $inactive   $bg         $fg         $inactive   $inactive
client.urgent             $orange     $bg         $fg         $orange     $orange
client.placeholder        $inactive   $bg         $fg         $inactive   $inactive
client.background         $bg

# SwayFX-specific settings
shadows enable
shadows_on_csd enable
shadow_blur_radius 20
shadow_color #00000088

blur enable
blur_passes 2
blur_radius 5
